$On=$true
$On.GetType().name
$num=1
$num.GetType().name
$a=1.5
$a.GetType().name
$flt=0.1234
$flt.GetType().name
$color='blue'
$color.GetType().name
//COncatenating Strings and Variables
$sentence="Powershell loves the color $color"
$sentence

